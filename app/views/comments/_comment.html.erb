  
 <div class="commentWrapper">
	 <div>
		<%= comment.body %>
	 </div>
	 <div class="commentInfo">
		 <span><%= comment.created_at %></span>
		 <span><%= link_to comment.commenter, show_path(current_admin.id) %></span>
	 </div>
</div>
    


<% if current_admin %>
<p>
  <%= link_to 'Löschen', [comment.game, comment],
               method: :delete,
               data: { confirm: 'Are you sure?' },
			   class: "btn btn-danger" %>
</p>
<% end %>